# Stadio sensori (physiological_state.py, build_dataset.py)
numpy>=1.26
pandas>=2.0

# Strato simbolico (symbolic.py): safety-state via query SPARQL sull'ontologia
# (ontology/runner_state.owl) + validazione NLP via SHACL (ontology/nlp_shapes.ttl).
# Solo Python puro, nessuna dipendenza Java/JVM.
rdflib>=7.0
pyshacl>=0.25

# Stadio NLP (intent.py, train_intent.py): SetFit few-shot classifica goal e mood.
# NB: SetFit e' un Transformer -> tira dentro torch. Su Python 3.9: sentence-transformers<5.
setfit>=1.1
sentence-transformers<5
datasets>=2.16
scikit-learn>=1.4
